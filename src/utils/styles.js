import { Colors } from "./colors";

export const customHeader = {
    headerStyle: {
        backgroundColor: Colors.primary,
    },
    headerTintColor: '#000',
    headerTitleStyle: {
        fontWeight: 'bold',
    }
}